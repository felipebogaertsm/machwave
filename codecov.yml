coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 10%
        removed_code_behavior: fully_covered_patch

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
